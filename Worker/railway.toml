# Serviço Worker (Python + demoparser2)
# No dashboard Railway: Root Directory = Worker

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
