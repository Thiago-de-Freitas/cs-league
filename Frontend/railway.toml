# Serviço frontend separado (opcional)
# Railway: Root Directory = Frontend

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
startCommand = "node serve.prod.cjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
