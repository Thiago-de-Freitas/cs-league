# gamers-league-front — variáveis do serviço frontend (Root Directory = Frontend)
# Importar: railway variable import --file railway.env --service gamers-league-front --yes
#
# NÃO inclua DATABASE_URL, JWT_SECRET, REDIS_URL ou CORS_ORIGIN aqui.

NODE_ENV=production
API_URL=https://${{gamers-league-back.RAILWAY_PUBLIC_DOMAIN}}
