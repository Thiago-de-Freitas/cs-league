# cs-league-front — variáveis do serviço frontend (Root Directory = Frontend)
# Importar: railway variable import --file railway.env --service cs-league-front --yes
#
# NÃO inclua DATABASE_URL, JWT_SECRET, REDIS_URL ou CORS_ORIGIN aqui.

NODE_ENV=production
API_URL=https://${{cs-league-back.RAILWAY_PUBLIC_DOMAIN}}
